Para executar a aplicação temos uma pasta com o spring e outra pasta com o react.

Para a pasta spring basta utilizar o src e o pom e 
criar o projeto no eclipse e executar o application.

Para o react  executar os comandos:
npm update
npm start


Assim que o spring e o react já estiverem ativados e só ir para o url http://localhost:3000